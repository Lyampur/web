= form_for @cinema_movie do |f|
  - if @cinema_movie.errors.any?
    #error_explanation
      %h2= "#{pluralize(@cinema_movie.errors.count, "error")} prohibited this cinema_movie from being saved:"
      %ul
        - @cinema_movie.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :cinema
    = f.text_field :cinema
  .field
    = f.label :movie
    = f.text_field :movie
  .actions
    = f.submit nil, class: 'btn btn-primary'
