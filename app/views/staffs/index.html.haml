%h1 Список работников
.table-responsive
  %table.table.table-hover.table-striped.center
    %thead
      %tr
        %th Фамилия
        %th Имя
        %th Дата рождения
        %th
        %th
        %th

    %tbody
      - @staffs.each do |staff|
        %tr
          %td= staff.last_name
          %td= staff.first_name
          %td= staff.birthday
          %td
            = link_to staff, class: 'btn btn-success' do
              = fa_icon :eye
          %td
            = link_to edit_staff_path(staff), class: 'btn btn-info' do
              = fa_icon :edit
          %td
            = link_to staff, :method => :delete, :data => { :confirm => t('helpers.sure') }, class: 'btn btn-danger' do
              = fa_icon :times

  %br

  = link_to 'Новый работник', new_staff_path
